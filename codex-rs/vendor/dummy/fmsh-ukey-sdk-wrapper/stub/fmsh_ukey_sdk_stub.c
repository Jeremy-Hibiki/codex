/* Empty stub for the FMSH UKey SDK shared library. The offline placeholder
 * crates never call SDK FFI symbols, so the library only needs to exist for
 * the linker. */
void fmsh_ukey_sdk_placeholder(void) {}
